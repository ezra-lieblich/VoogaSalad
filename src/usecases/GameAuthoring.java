package usecases;

public class GameAuthoring {
	/**
	void userTappedCell(Coordinates coord){
		if (this.pathEditing){
			this.pathCoordinates.add(coord);
			boolean pathIsValid = mapStateUpdater.updatePath(pathCoordinates);
			if (!pathIsValid){
				this.errorPresenter.presentInvalidPathError();
			}
		}
	}
	*/
}
